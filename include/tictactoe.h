#ifndef TICTACTOE_H   // This ensures the file is only included once to prevent errors.
#define TICTACTOE_H   // Deklariert idfk
#define BOARD_SIZE 3   // Defining the size of the Tic-Tac-Toe board (3x3).

#endif
